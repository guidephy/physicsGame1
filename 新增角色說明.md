# 新增物理學家

角色名單、圖鑑、選角按鈕由資料自動產生，不限制為六人。

## 1. 準備角色圖片

將原創或已取得使用權的 PNG 圖集放在 `assets`。每格大小一致、人物朝右、腳底對齊。現在的四列依序是：站姿、出拳、踢擊、施法；渲染器會依動作切換列。新人物可以用自己的圖集，不必重新繪製現有六人。

例如單人四列圖片使用 `columns: 1, rows: 4, column: 0`。每格可以是 256 × 256。避免衣服與手腳超出格子。

## 2. 加入角色資料

在 `game/roster.js` 的 FIGHTERS 陣列加入一筆。參考現有角色並填寫：

- `id`：不重複的英文代號。
- `name`、`english`、`years`、`title`：人物名稱、年代與外觀辨識資訊。
- `discipline`、`fact`、`formula`：領域、核實後的科學貢獻與代表公式。
- `color`、`style`：特效顏色與玩法介紹。
- `speed`、`power`：可先使用 260 與 1，再依測試調整。
- `sprite`：圖片來源與圖集格數。
- `special`、`ultimate`：名稱、機制類型、玩家能理解的效果說明。

圖片來源使用 `src: './assets/檔名.png'`，即可同時支援直接開啟檔案、GitHub Pages 根網站與儲存庫子路徑。所有程式透過一般 script 依序載入，使用 `globalThis.PhysicsArena` 分享介面；不需要 import、打包或建置工具。

現有可重用的機制：

| kind | 機制 |
| --- | --- |
| gravity | 落體與引力區域 |
| coil | 延遲放電與線圈區域 |
| orbit | 沿軌道往返的投射物 |
| interference | 雙波與交替干涉區域 |
| energy | 固定距離位移與連續光子 |
| photon | 快速光子與減速區域 |

## 3. 全新招式機制

若現有機制不適合，在 `game/engine.js` 的 `SKILLS` 物件加入新名稱與函式，再把角色的 `kind` 設為該名稱。函式收到對戰狀態、玩家編號、是否大絕招，以及雙方人物狀態。

以 `m.effect(...)` 建立投射物或區域；傷害交給 `m.hit(...)` 統一處理，才能保留防禦、受擊保護、能量與回合規則。需要新特效時，再擴充 `game/render.js` 的 `effect` 方法。不要在選角介面中寫傷害邏輯。

## 4. 增加動畫影格

目前四列圖集只是第一版規格。若要支援完整走路／跳躍／受擊動畫，可在 `sprite` 資料新增 `animations` 設定（影格位置、播放時間、是否循環），再讓 `render.js` 依角色狀態與 elapsed 取格。保持繪圖與命中時間獨立，避免美術影格數改變攻擊規則。

## 5. 驗收

新增人物後測試選角、左右面向、基本攻防、小絕招、大絕招、能量、冷卻、跳躍躲避、回合勝負、手機觸控與角色配對。測試檔中的固定角色數預期需同步更新；配對測試會自動遍歷新名單。
